dataset: csl
variant: concat
run_name: csl_concat
train:
  epochs: 200
  patience: 50
  seed: 42
  batch_size: 16
  monitor: val_acc
optim:
  lr: 0.01
  weight_decay: 0.0
model:
  hidden_dim: 64
  num_layers: 4
  dropout: 0.0
