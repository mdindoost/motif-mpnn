dataset: nci1
variant: mix
run_name: nci1_mix
mix:
  lambda_mix: 0.25
  motif_topk: 10
  sim_metric: cosine
  self_loop: true
train:
  epochs: 200
  patience: 50
  seed: 42
  batch_size: 64
optim:
  lr: 0.01
  weight_decay: 0.0
