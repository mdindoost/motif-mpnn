concat:
  motif_scale: 1.0
dataset: proteins
motif_features: orbit
motif_rand: true
optim:
  lr: 0.01
  weight_decay: 0.0
run_name: proteins_orbit_rand_concat
train:
  batch_size: 64
  epochs: 200
  patience: 50
  seed: 42
variant: concat
