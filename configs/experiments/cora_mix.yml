# Experiment: Cora + Mix (A and A_motif mixing)
dataset: cora
variant: mix
use_motif_edge: false
use_A_motif: true
lambda:
  mode: scalar   # scalar | nodewise
  value: 0.25
pruning:
  node_topk: 32
  edge_topk: 0
normalize:
  log1p: true
  zscore: true
  degree_norm: false
