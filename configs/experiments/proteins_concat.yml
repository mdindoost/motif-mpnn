dataset: proteins
variant: concat
run_name: proteins_concat
concat:
  motif_scale: 1.0    # (optional) γ · motif_x before concat
train:
  epochs: 200
  patience: 50
  seed: 42
  batch_size: 64
optim:
  lr: 0.01
  weight_decay: 0.0
