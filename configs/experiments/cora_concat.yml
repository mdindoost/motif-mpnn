# Minimal experiment that validates Phase A plumbing using the dummy dataset.
# In Phase B we’ll switch dataset.name → 'cora' and remove the stub.
dataset:
name: dummy_node
task: node
root: data/processed


model:
name: identity
hidden_dim: 64
num_layers: 2
dropout: 0.5


run_name: phaseA_smoke
