# Preprocessing dependencies (for scripts/preprocess/)
# Install in addition to requirements.txt:
#   pip install -r requirements-preprocess.txt

python-igraph==1.0.0   # required for --tool igraph (default backend)
networkit==11.2.1       # required for --tool networkit (optional, faster on large graphs)
ogb==1.3.6              # required for ogbn-arxiv dataset loading (future)
tqdm==4.67.1            # progress bars (also used in training; pin here for completeness)
